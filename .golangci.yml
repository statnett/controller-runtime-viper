---
version: "2"
linters:
  enable:
    - errname
    - errorlint
    - goconst
    - gocritic
    - godot
    - gosec
    - modernize
    - predeclared
    - unparam
    - whitespace
    - wsl_v5
  settings:
    gocritic:
      enabled-checks:
        - importShadow
formatters:
  enable:
    - gofmt
    - goimports
  settings:
    goimports:
      local-prefixes:
        - github.com/statnett/controller-runtime-viper
